# Visual Asset Rules

1. 기본값은 직접 생성한 Mermaid diagram을 사용한다.
2. 외부 이미지는 license와 source attribution이 명확한 경우에만 저장한다.
3. License가 불명확한 이미지는 복사하지 않고 자체 diagram으로 대체한다.
4. 모든 asset은 topic folder의 `assets/` 아래에 저장한다.
5. 모든 asset metadata는 `visual_assets.json`에 기록한다.
